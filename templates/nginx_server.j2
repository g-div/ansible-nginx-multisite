#Ansible managed

server {
    listen   80;
    server_name {{ subdomain }}.{{ domain }};
    root {{ folder }}/{{ default_static }};
    index index.html index.htm;
    include {{ nginx_dir }}/sites/*.conf;
}
